// Feature flags — flip FANTASY_ENABLED = true to re-enable the Fantasy Game for all users.
// Admins always bypass this flag and can access Fantasy pages regardless.
export const FANTASY_ENABLED = false;